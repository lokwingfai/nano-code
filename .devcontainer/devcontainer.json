{
  "name": "nano-code-devcontainer",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/nano-code",
  "remoteUser": "vscode",
  "mounts": [
    "source=${localWorkspaceFolder},target=/nano-code,type=bind"
  ],
  "postCreateCommand": "if [ ! -f package.json ]; then bun init -y; fi && bun install"
}
